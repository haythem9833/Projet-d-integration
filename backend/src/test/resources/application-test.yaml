server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/elearning_test?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    database-platform: org.hibernate.dialect.MySQLDialect

app:
  jwt:
    secret: myVeryLongSecretKeyThatIsAtLeast256BitsLongForHS256AlgorithmSecurityCompliance123456789012345678901234567890

stripe:
  api:
    key: sk_test_your_test_key_here
